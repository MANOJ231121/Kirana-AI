services:
  mongodb:
    image: mongo:7
    container_name: kirana-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - kirana-mongo-data:/data/db

volumes:
  kirana-mongo-data:
